# Changelog

## Unreleased

- Added the Fastly API MCP adapter, full-coverage request tool, tenant-scoped user token persistence, dedicated service/version/purge tools, endpoint inventory generation, and Fastly-specific tests.
